# Codegen

Multi-language code generation CLI (TypeScript, Python, ...) by templates.

## Install

Works in any Node project after installing via:

``` sh
npm install -D github:serhii-untilov/codegen
```

## Create a template

Create a `templates` folder in the root of your project.

Create a template file, like a `templates\python.class\name.snakeCase.py.hbs`

``` hbs
# Generated by {{codegen.name}} v{{codegen.version}} at {{codegen.date}} {{codegen.time}}
# Homepage: {{codegen.homepage}}
# Template: {{codegen.template}}

class {{pascalCase name}}:
    def __init__(self):
        pass
```

## Generate code

To generate code run:

``` sh
npx codegen --templates ./templates/python.class --name example --output src
```

The Codegen will generate the file `src\example.py`

## Getting help

``` sh
npx codegen --help
npx codegen --version
```

## Update

``` sh
npm update codegen
```

## Development

``` sh
npm run build
./dist/app.js --templates ./templates/ts.class --output ./out
```
